<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.blog_froum.mapper.UserFollowMapper">

    <!-- 添加关注关系 -->
    <insert id="insert" parameterType="com.example.blog_froum.entity.UserFollow" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_follows (follower_id, following_id, created_at, updated_at)
        VALUES (#{followerId}, #{followingId}, #{createdAt}, #{updatedAt})
    </insert>

    <!-- 删除关注关系 -->
    <delete id="delete">
        DELETE FROM user_follows 
        WHERE follower_id = #{followerId} AND following_id = #{followingId}
    </delete>

    <!-- 查询关注关系是否存在 -->
    <select id="exists" resultType="boolean">
        SELECT COUNT(*) > 0 
        FROM user_follows 
        WHERE follower_id = #{followerId} AND following_id = #{followingId}
    </select>

    <!-- 获取用户的关注者（粉丝） -->
    <select id="findFollowers" resultMap="com.example.blog_froum.mapper.UserMapper.UserResultMap">
        SELECT u.*
        FROM users u
        JOIN user_follows uf ON u.id = uf.follower_id
        WHERE uf.following_id = #{userId}
        ORDER BY uf.created_at DESC
        LIMIT #{offset}, #{limit}
    </select>

    <!-- 获取用户关注的人 -->
    <select id="findFollowing" resultMap="com.example.blog_froum.mapper.UserMapper.UserResultMap">
        SELECT u.*
        FROM users u
        JOIN user_follows uf ON u.id = uf.following_id
        WHERE uf.follower_id = #{userId}
        ORDER BY uf.created_at DESC
        LIMIT #{offset}, #{limit}
    </select>

    <!-- 统计用户的关注者数量（粉丝数） -->
    <select id="countFollowers" resultType="long">
        SELECT COUNT(*) 
        FROM user_follows 
        WHERE following_id = #{userId}
    </select>

    <!-- 统计用户关注的人数 -->
    <select id="countFollowing" resultType="long">
        SELECT COUNT(*) 
        FROM user_follows 
        WHERE follower_id = #{userId}
    </select>

</mapper> 